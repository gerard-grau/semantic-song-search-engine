# ── Core API + retrieval stack ───────────────────────────────────────────────
# Backend
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0

# Data / numerics
pandas>=2.2,<3.0
pyarrow>=15.0,<16.0
numpy>=1.26,<2.0
scikit-learn>=1.4,<2.0
umap-learn>=0.5,<1.0

# Embeddings (query encoding + cross-encoder reranking)
torch>=2.4,<3.0
transformers>=4.44,<5.0
sentence-transformers>=3.0,<4.0

# Vector DB client (connects to Docker-hosted Qdrant)
qdrant-client>=1.9,<2.0

# Cercador — Catalan lexicon for typo correction
wordfreq>=3.1,<4.0

# Data pipeline — step1 fetches catalogue CSVs from MariaDB
pymysql>=1.1,<2.0
python-dotenv>=1.0,<2.0

# Optional: extras for the YouTube audio pipeline are in
# youtube_audio_pipeline/requirements.txt (essentia + tensorflow + yt-dlp).
